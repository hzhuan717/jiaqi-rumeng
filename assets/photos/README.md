# 素材放置说明

## 照片（相册页自动展示）

把照片放到本目录，命名为 `01.jpg` ~ `06.jpg`，打开「时光相册」章节就会自动显示。

- 格式：jpg（也可以直接改成 png，但文件名要对应）
- 建议：竖版 3:4，每张 1MB 以内，加载更快
- 不想显示哪张，删除对应文件即可

## 背景音乐

把音乐文件放到 `../music/music.mp3`，右上角音符按钮即可播放背景音乐。

- 建议 mp3 格式，2-5MB，会循环播放
- 不放也不影响体验，页面会自动隐藏音乐按钮并提示
